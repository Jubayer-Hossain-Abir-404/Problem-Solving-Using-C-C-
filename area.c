#include<stdio.h>
int main()
{
    float length,width;
    printf("Enter the value of length and width:");
    scanf("%f%f",&length,&width);
    printf("The area is %.2f",length*width);
    return 0;
}
